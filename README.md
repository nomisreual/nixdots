# Personal Configuration Files

These are my personal configuration files for systems running NixOS. System level configurations are placed in *system*, home-manager configurations can be
found in *home*. The configuration files of quite a few applications are not (yet) managed the "nix" way, but I opted to use home-manager to create symlinks for these
applications rather than using stow.
